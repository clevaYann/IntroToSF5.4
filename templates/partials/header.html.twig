<nav class="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
        <a href="/" class="navbar-brand">IRISOF</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mobile-nav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a href="{{ path('home') }}" class="nav-link">Acceil</a>
                </li>
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Articles</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('article_index') }}" class="dropdown-item">Liste des articles</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('article_new') }}" class="dropdown-item">Ajouter un article</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown">Catégories</a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('category_index') }}" class="dropdown-item">Liste des catégories</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('category_new') }}" class="dropdown-item">Ajouter une catégorie</a>
                        <div class="dropdown-divider"></div>
                        <a href="{{ path('article_in_cat') }}" class="dropdown-item">Recherche article par catégorie</a>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>