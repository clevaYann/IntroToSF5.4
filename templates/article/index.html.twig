{% extends 'base.html.twig' %}
{% block title %}Articles{% endblock %}

{% block body %}

    <table id="articles" class="table table-striped">

        <thead>
        <tr>

            <th>Article</th>
            <th>Prix</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>

        {% for article in articles %}
            <tr>
                <td>{{ article.name }}</td>
                <td>{{ article.price }}</td>
                <td><a href="/article/{{ article.id }}" class="btn btn-info">Détails</a></td>
            </tr> {% endfor %}
        </tbody>
    </table>

{% endblock %}
